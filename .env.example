# Copy values into Cursor mcp.json env (do not commit real secrets).
MS_CLIENT_ID=your-application-client-id
MS_TENANT_ID=your-directory-tenant-id
# Optional. Device-code auth does not need a client secret.
# MS_CLIENT_SECRET=
